import streamlit as st

# إعدادات الصفحة
st.set_page_config(page_title="التوأم الصحي الرقمي", page_icon="🧬")

# رسالة ترحيبية
st.title("🧬 توأمك الصحي الافتراضي")
st.write("هذا التطبيق يربط عاداتك اليومية بحالة توأمك الرقمي.")

# مدخلات المستخدم
with st.sidebar:
    st.header("سجل نشاطك اليومي")
    water = st.slider("كم كوب ماء شربت اليوم؟", 0, 12, 0)
    exercise = st.checkbox("هل مارست الرياضة اليوم؟")
    sleep = st.number_input("ساعات النوم", min_value=0, max_value=24, value=7)

# منطق التوأم الرقمي (Logic)
st.subheader("حالة توأمك الآن:")

if water < 4:
    st.error("🔴 توأمك يعاني من الجفاف! بشرته شاحبة وطاقته منخفضة.")
    st.info("نصيحة: اشرب كوبين من الماء الآن لتحسين حالته.")
elif 4 <= water < 8:
    st.warning("🟡 توأمك في حالة مستقرة، لكنه يحتاج لمزيد من الارتواء.")
else:
    st.success("🟢 توأمك مشرق ونشيط جداً! أنت تقوم بعمل رائع.")

if exercise:
    st.balloons()
    st.write("💪 توأمك يبني عضلات الآن ويشعر بالقوة!")

# إخلاء مسؤولية
st.caption("---")
st.caption("ملاحظة: هذا التطبيق تعليمي وتوعوي ولا يعتبر نصيحة طبية متخصصة.")
